import 'package:flutter/material.dart';

class PhotosProperties extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 50.0,
      child: Text('Image selected'),
    );
  }
}
